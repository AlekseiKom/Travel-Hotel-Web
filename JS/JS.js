/* $(document).ready(function() {
    var freqSecs = 1.2;
    setInterval (blink, freqSecs*1000 );
    function blink() {
     var inout = (freqSecs*1000)/0.5;
     $("#ask").fadeIn(inout).fadeOut(inout);
    }
   }); */